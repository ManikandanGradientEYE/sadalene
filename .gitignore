# Build output
bin/
obj/

# Visual Studio local files
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet packages (restored automatically)
packages/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Publish output
publish/
**/publish/

# ASP.NET Core runtime
Temporary ASP.NET Files/
*.log

# Rider / JetBrains
.idea/
*.DotSettings.user

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Environment / secrets
appsettings.Production.json
.env
*.pfx
*.key

# Static asset cache
wwwroot/.cache/

# Entity Framework migrations designer snapshots (keep .cs, ignore .Designer.cs if desired)
# Uncomment below if you want to exclude migration designer files:
# **/Migrations/*.Designer.cs

# ---- Node / Expo / React Native (Sadalene.Mobile) ----
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
npm-debug.*
yarn-debug.*
yarn-error.*
*.orig.*
*.jks
*.p8
*.p12
*.mobileprovision
.env.local
.env.*.local
*.tsbuildinfo

# React Native / native build dirs (managed workflow shouldn't generate these unless prebuilt/ejected — ignore defensively)
ios/Pods/
ios/build/
android/.gradle/
android/build/
android/app/build/
android/local.properties
